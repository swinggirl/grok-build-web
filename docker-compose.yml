services:
  grok-build-web:
    image: node:20-alpine
    container_name: grok-build-web
    restart: unless-stopped
    ports:
      - "5585:5585"
    volumes:
      - ./app:/app
    working_dir: /app
    command: sh -c "npm install && node server.js"